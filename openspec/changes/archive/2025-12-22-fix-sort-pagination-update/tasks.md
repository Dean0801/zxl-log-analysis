## 1. 问题诊断
- [x] 1.1 检查 `handleSortOrderChange` 函数的执行流程
- [x] 1.2 检查 `applyFilters` 函数中排序和分页重置的逻辑
- [x] 1.3 验证 `renderTable` 和 `renderPagination` 是否在排序后正确调用
- [x] 1.4 检查是否存在异步操作或状态同步问题

## 2. 修复实现
- [x] 2.1 确保排序切换时正确重置 `currentPage` 为 1
- [x] 2.2 确保 `renderTable` 使用更新后的 `filteredData` 和 `currentPage`
- [x] 2.3 确保 `renderPagination` 使用更新后的 `currentPage` 和总页数
- [x] 2.4 添加必要的状态同步检查，确保渲染时数据已更新

## 3. 测试验证
- [x] 3.1 测试在第2页切换排序，验证页面是否正确更新到第1页
- [x] 3.2 测试在第3页或更后面的页面切换排序，验证页面更新
- [x] 3.3 测试切换排序后，表格内容是否正确显示重新排序后的数据
- [x] 3.4 测试切换排序后，分页控件是否正确显示当前页码和总页数
- [x] 3.5 测试在有筛选条件的情况下切换排序，验证筛选状态是否保持

